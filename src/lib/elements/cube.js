import {
  VertexArray,
  VertexBufferLayout
} from '../gl/vertex-array/vertexArray.js'

class Cube {
  vertexShader = `#version 300 es
  layout(location = 0) in vec3 position;
  uniform mat4 u_proj;
  uniform mat4 u_view;
  
  void main(void) {
    
  }
  `;
  constructor(gl, ) {
    
  }
};